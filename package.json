{
  "name": "eryndor-essentials",
  "version": "1.1.0",
  "description": "Maiyalis: Utility Suite — quality-of-life tools for the Eryndor campaign in FoundryVTT.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "watch": "vite build --watch",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
